# API Reference (Python)

The Python SDK exposes generated protobuf modules under the `virtengine` package
and convenience wrappers in `virtengine.modules`.

Key entry points:

- `virtengine.VirtEngineClient`
- `virtengine.Wallet`
- `virtengine.modules.ModuleRegistry`
