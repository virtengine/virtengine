import { bytesFromBase64, base64FromBytes, isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/marketplace/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

/** MsgWaldurCallback handles callbacks from Waldur integration */
export interface MsgWaldurCallback {
  sender: string;
  callbackType: string;
  resourceId: string;
  status: string;
  payload: string;
  signature: Uint8Array;
}

/** MsgWaldurCallbackResponse is the response for MsgWaldurCallback */
export interface MsgWaldurCallbackResponse {
}

function createBaseMsgWaldurCallback(): MsgWaldurCallback {
  return { sender: "", callbackType: "", resourceId: "", status: "", payload: "", signature: new Uint8Array(0) };
}

export const MsgWaldurCallback: MessageFns<MsgWaldurCallback, "virtengine.marketplace.v1.MsgWaldurCallback"> = {
  $type: "virtengine.marketplace.v1.MsgWaldurCallback" as const,

  encode(message: MsgWaldurCallback, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.callbackType !== "") {
      writer.uint32(18).string(message.callbackType);
    }
    if (message.resourceId !== "") {
      writer.uint32(26).string(message.resourceId);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    if (message.payload !== "") {
      writer.uint32(42).string(message.payload);
    }
    if (message.signature.length !== 0) {
      writer.uint32(50).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgWaldurCallback {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgWaldurCallback();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.callbackType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resourceId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgWaldurCallback {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      callbackType: isSet(object.callback_type) ? globalThis.String(object.callback_type) : "",
      resourceId: isSet(object.resource_id) ? globalThis.String(object.resource_id) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      payload: isSet(object.payload) ? globalThis.String(object.payload) : "",
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgWaldurCallback): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.callbackType !== "") {
      obj.callback_type = message.callbackType;
    }
    if (message.resourceId !== "") {
      obj.resource_id = message.resourceId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.payload !== "") {
      obj.payload = message.payload;
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<MsgWaldurCallback>): MsgWaldurCallback {
    const message = createBaseMsgWaldurCallback();
    message.sender = object.sender ?? "";
    message.callbackType = object.callbackType ?? "";
    message.resourceId = object.resourceId ?? "";
    message.status = object.status ?? "";
    message.payload = object.payload ?? "";
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgWaldurCallbackResponse(): MsgWaldurCallbackResponse {
  return {};
}

export const MsgWaldurCallbackResponse: MessageFns<
  MsgWaldurCallbackResponse,
  "virtengine.marketplace.v1.MsgWaldurCallbackResponse"
> = {
  $type: "virtengine.marketplace.v1.MsgWaldurCallbackResponse" as const,

  encode(_: MsgWaldurCallbackResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgWaldurCallbackResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgWaldurCallbackResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgWaldurCallbackResponse {
    return {};
  },

  toJSON(_: MsgWaldurCallbackResponse): unknown {
    const obj: any = {};
    return obj;
  },
  fromPartial(_: DeepPartial<MsgWaldurCallbackResponse>): MsgWaldurCallbackResponse {
    const message = createBaseMsgWaldurCallbackResponse();
    return message;
  },
};

function _unused_bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function _unused_base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
