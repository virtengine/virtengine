import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/fraud/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Params } from "./params.ts";
import { FraudAuditLog, FraudReport, ModeratorQueueEntry } from "./types.ts";

/** GenesisState defines the fraud module's genesis state */
export interface GenesisState {
  /** Params defines the module parameters */
  params:
    | Params
    | undefined;
  /** Reports contains all fraud reports */
  reports: FraudReport[];
  /** AuditLogs contains all audit log entries */
  auditLogs: FraudAuditLog[];
  /** ModeratorQueue contains all queue entries */
  moderatorQueue: ModeratorQueueEntry[];
  /** NextFraudReportSequence is the next fraud report sequence number */
  nextFraudReportSequence: Long;
  /** NextAuditLogSequence is the next audit log sequence number */
  nextAuditLogSequence: Long;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    reports: [],
    auditLogs: [],
    moderatorQueue: [],
    nextFraudReportSequence: Long.UZERO,
    nextAuditLogSequence: Long.UZERO,
  };
}

export const GenesisState: MessageFns<GenesisState, "virtengine.fraud.v1.GenesisState"> = {
  $type: "virtengine.fraud.v1.GenesisState" as const,

  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.reports) {
      FraudReport.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.auditLogs) {
      FraudAuditLog.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.moderatorQueue) {
      ModeratorQueueEntry.encode(v!, writer.uint32(34).fork()).join();
    }
    if (!message.nextFraudReportSequence.equals(Long.UZERO)) {
      writer.uint32(40).uint64(message.nextFraudReportSequence.toString());
    }
    if (!message.nextAuditLogSequence.equals(Long.UZERO)) {
      writer.uint32(48).uint64(message.nextAuditLogSequence.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reports.push(FraudReport.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.auditLogs.push(FraudAuditLog.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.moderatorQueue.push(ModeratorQueueEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.nextFraudReportSequence = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.nextAuditLogSequence = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      reports: globalThis.Array.isArray(object?.reports) ? object.reports.map((e: any) => FraudReport.fromJSON(e)) : [],
      auditLogs: globalThis.Array.isArray(object?.audit_logs)
        ? object.audit_logs.map((e: any) => FraudAuditLog.fromJSON(e))
        : [],
      moderatorQueue: globalThis.Array.isArray(object?.moderator_queue)
        ? object.moderator_queue.map((e: any) => ModeratorQueueEntry.fromJSON(e))
        : [],
      nextFraudReportSequence: isSet(object.next_fraud_report_sequence)
        ? Long.fromValue(object.next_fraud_report_sequence)
        : Long.UZERO,
      nextAuditLogSequence: isSet(object.next_audit_log_sequence)
        ? Long.fromValue(object.next_audit_log_sequence)
        : Long.UZERO,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.reports?.length) {
      obj.reports = message.reports.map((e) => FraudReport.toJSON(e));
    }
    if (message.auditLogs?.length) {
      obj.audit_logs = message.auditLogs.map((e) => FraudAuditLog.toJSON(e));
    }
    if (message.moderatorQueue?.length) {
      obj.moderator_queue = message.moderatorQueue.map((e) => ModeratorQueueEntry.toJSON(e));
    }
    if (!message.nextFraudReportSequence.equals(Long.UZERO)) {
      obj.next_fraud_report_sequence = (message.nextFraudReportSequence || Long.UZERO).toString();
    }
    if (!message.nextAuditLogSequence.equals(Long.UZERO)) {
      obj.next_audit_log_sequence = (message.nextAuditLogSequence || Long.UZERO).toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.reports = object.reports?.map((e) => FraudReport.fromPartial(e)) || [];
    message.auditLogs = object.auditLogs?.map((e) => FraudAuditLog.fromPartial(e)) || [];
    message.moderatorQueue = object.moderatorQueue?.map((e) => ModeratorQueueEntry.fromPartial(e)) || [];
    message.nextFraudReportSequence =
      (object.nextFraudReportSequence !== undefined && object.nextFraudReportSequence !== null)
        ? Long.fromValue(object.nextFraudReportSequence)
        : Long.UZERO;
    message.nextAuditLogSequence = (object.nextAuditLogSequence !== undefined && object.nextAuditLogSequence !== null)
      ? Long.fromValue(object.nextAuditLogSequence)
      : Long.UZERO;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
