import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/enclave/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { EnclaveIdentity, KeyRotationRecord, MeasurementRecord, Params } from "./types.ts";

/** GenesisState defines the enclave module's genesis state */
export interface GenesisState {
  /** EnclaveIdentities are the registered enclave identities */
  enclaveIdentities: EnclaveIdentity[];
  /** MeasurementAllowlist is the list of approved enclave measurements */
  measurementAllowlist: MeasurementRecord[];
  /** KeyRotations is the list of active key rotations */
  keyRotations: KeyRotationRecord[];
  /** Params are the module parameters */
  params: Params | undefined;
}

function createBaseGenesisState(): GenesisState {
  return { enclaveIdentities: [], measurementAllowlist: [], keyRotations: [], params: undefined };
}

export const GenesisState: MessageFns<GenesisState, "virtengine.enclave.v1.GenesisState"> = {
  $type: "virtengine.enclave.v1.GenesisState" as const,

  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.enclaveIdentities) {
      EnclaveIdentity.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.measurementAllowlist) {
      MeasurementRecord.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.keyRotations) {
      KeyRotationRecord.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.enclaveIdentities.push(EnclaveIdentity.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.measurementAllowlist.push(MeasurementRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.keyRotations.push(KeyRotationRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      enclaveIdentities: globalThis.Array.isArray(object?.enclave_identities)
        ? object.enclave_identities.map((e: any) => EnclaveIdentity.fromJSON(e))
        : [],
      measurementAllowlist: globalThis.Array.isArray(object?.measurement_allowlist)
        ? object.measurement_allowlist.map((e: any) => MeasurementRecord.fromJSON(e))
        : [],
      keyRotations: globalThis.Array.isArray(object?.key_rotations)
        ? object.key_rotations.map((e: any) => KeyRotationRecord.fromJSON(e))
        : [],
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.enclaveIdentities?.length) {
      obj.enclave_identities = message.enclaveIdentities.map((e) => EnclaveIdentity.toJSON(e));
    }
    if (message.measurementAllowlist?.length) {
      obj.measurement_allowlist = message.measurementAllowlist.map((e) => MeasurementRecord.toJSON(e));
    }
    if (message.keyRotations?.length) {
      obj.key_rotations = message.keyRotations.map((e) => KeyRotationRecord.toJSON(e));
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.enclaveIdentities = object.enclaveIdentities?.map((e) => EnclaveIdentity.fromPartial(e)) || [];
    message.measurementAllowlist = object.measurementAllowlist?.map((e) => MeasurementRecord.fromPartial(e)) || [];
    message.keyRotations = object.keyRotations?.map((e) => KeyRotationRecord.fromPartial(e)) || [];
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
