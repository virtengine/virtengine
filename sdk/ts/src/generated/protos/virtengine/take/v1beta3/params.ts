import { isSet } from "..\..\..\..\..\encoding\typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "..\..\..\..\..\encoding\typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/take/v1beta3/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

/** DenomTakeRate describes take rate for specified denom */
export interface DenomTakeRate {
  denom: string;
  rate: number;
}

/** Params defines the parameters for the x/take package */
export interface Params {
  /** denom -> % take rate */
  denomTakeRates: DenomTakeRate[];
  defaultTakeRate: number;
}

function createBaseDenomTakeRate(): DenomTakeRate {
  return { denom: "", rate: 0 };
}

export const DenomTakeRate: MessageFns<DenomTakeRate, "virtengine.take.v1beta3.DenomTakeRate"> = {
  $type: "virtengine.take.v1beta3.DenomTakeRate" as const,

  encode(message: DenomTakeRate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.denom !== "") {
      writer.uint32(10).string(message.denom);
    }
    if (message.rate !== 0) {
      writer.uint32(16).uint32(message.rate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DenomTakeRate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDenomTakeRate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.denom = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rate = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DenomTakeRate {
    return {
      denom: isSet(object.denom) ? globalThis.String(object.denom) : "",
      rate: isSet(object.rate) ? globalThis.Number(object.rate) : 0,
    };
  },

  toJSON(message: DenomTakeRate): unknown {
    const obj: any = {};
    if (message.denom !== "") {
      obj.denom = message.denom;
    }
    if (message.rate !== 0) {
      obj.rate = Math.round(message.rate);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<DenomTakeRate>): DenomTakeRate {
    const message = createBaseDenomTakeRate();
    message.denom = object.denom ?? "";
    message.rate = object.rate ?? 0;
    return message;
  },
};

function createBaseParams(): Params {
  return { denomTakeRates: [], defaultTakeRate: 0 };
}

export const Params: MessageFns<Params, "virtengine.take.v1beta3.Params"> = {
  $type: "virtengine.take.v1beta3.Params" as const,

  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.denomTakeRates) {
      DenomTakeRate.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.defaultTakeRate !== 0) {
      writer.uint32(16).uint32(message.defaultTakeRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.denomTakeRates.push(DenomTakeRate.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.defaultTakeRate = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      denomTakeRates: globalThis.Array.isArray(object?.denom_take_rates)
        ? object.denom_take_rates.map((e: any) => DenomTakeRate.fromJSON(e))
        : [],
      defaultTakeRate: isSet(object.default_take_rate) ? globalThis.Number(object.default_take_rate) : 0,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.denomTakeRates?.length) {
      obj.denom_take_rates = message.denomTakeRates.map((e) => DenomTakeRate.toJSON(e));
    }
    if (message.defaultTakeRate !== 0) {
      obj.default_take_rate = Math.round(message.defaultTakeRate);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<Params>): Params {
    const message = createBaseParams();
    message.denomTakeRates = object.denomTakeRates?.map((e) => DenomTakeRate.fromPartial(e)) || [];
    message.defaultTakeRate = object.defaultTakeRate ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
