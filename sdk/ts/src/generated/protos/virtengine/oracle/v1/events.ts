// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/oracle/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { DataID, PriceDataState } from "./prices.ts";

/** EventPriceData is emitted when new price data is added to the oracle */
export interface EventPriceData {
  /** source is the address of the price source (oracle provider) */
  source: string;
  /** id identifies the price pair (denom and base_denom) */
  id:
    | DataID
    | undefined;
  /** data contains the price value and timestamp */
  data: PriceDataState | undefined;
}

/** EventPriceStaleWarning is emitted when price has not been updated and is about to become stale */
export interface EventPriceStaleWarning {
  /** source is the address of the price source */
  source: string;
  /** id identifies the price pair */
  id:
    | DataID
    | undefined;
  /** last_height is the block height when the price was last updated */
  lastHeight: Long;
  /** blocks_to_stall is the number of blocks until the price becomes stale */
  blocksToStall: Long;
}

/** EventPriceStaled is emitted when a price has become stale */
export interface EventPriceStaled {
  /** source is the address of the price source */
  source: string;
  /** id identifies the price pair */
  id:
    | DataID
    | undefined;
  /** last_height is the block height when the price was last updated before becoming stale */
  lastHeight: Long;
}

/** EventPriceRecovered is emitted when a stale price has started receiving updates again */
export interface EventPriceRecovered {
  /** source is the address of the price source */
  source: string;
  /** id identifies the price pair */
  id:
    | DataID
    | undefined;
  /** height is the block height when the price recovery was detected */
  height: Long;
}

function createBaseEventPriceData(): EventPriceData {
  return { source: "", id: undefined, data: undefined };
}

export const EventPriceData: MessageFns<EventPriceData, "virtengine.oracle.v1.EventPriceData"> = {
  $type: "virtengine.oracle.v1.EventPriceData" as const,

  encode(message: EventPriceData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.id !== undefined) {
      DataID.encode(message.id, writer.uint32(18).fork()).join();
    }
    if (message.data !== undefined) {
      PriceDataState.encode(message.data, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPriceData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPriceData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = DataID.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = PriceDataState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPriceData {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      id: isSet(object.id) ? DataID.fromJSON(object.id) : undefined,
      data: isSet(object.data) ? PriceDataState.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: EventPriceData): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.id !== undefined) {
      obj.id = DataID.toJSON(message.id);
    }
    if (message.data !== undefined) {
      obj.data = PriceDataState.toJSON(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<EventPriceData>): EventPriceData {
    return EventPriceData.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventPriceData>): EventPriceData {
    const message = createBaseEventPriceData();
    message.source = object.source ?? "";
    message.id = (object.id !== undefined && object.id !== null) ? DataID.fromPartial(object.id) : undefined;
    message.data = (object.data !== undefined && object.data !== null)
      ? PriceDataState.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseEventPriceStaleWarning(): EventPriceStaleWarning {
  return { source: "", id: undefined, lastHeight: Long.ZERO, blocksToStall: Long.ZERO };
}

export const EventPriceStaleWarning: MessageFns<EventPriceStaleWarning, "virtengine.oracle.v1.EventPriceStaleWarning"> =
  {
    $type: "virtengine.oracle.v1.EventPriceStaleWarning" as const,

    encode(message: EventPriceStaleWarning, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
      if (message.source !== "") {
        writer.uint32(10).string(message.source);
      }
      if (message.id !== undefined) {
        DataID.encode(message.id, writer.uint32(18).fork()).join();
      }
      if (!message.lastHeight.equals(Long.ZERO)) {
        writer.uint32(24).int64(message.lastHeight.toString());
      }
      if (!message.blocksToStall.equals(Long.ZERO)) {
        writer.uint32(32).int64(message.blocksToStall.toString());
      }
      return writer;
    },

    decode(input: BinaryReader | Uint8Array, length?: number): EventPriceStaleWarning {
      const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventPriceStaleWarning();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.source = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.id = DataID.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.lastHeight = Long.fromString(reader.int64().toString());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.blocksToStall = Long.fromString(reader.int64().toString());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): EventPriceStaleWarning {
      return {
        source: isSet(object.source) ? globalThis.String(object.source) : "",
        id: isSet(object.id) ? DataID.fromJSON(object.id) : undefined,
        lastHeight: isSet(object.last_height) ? Long.fromValue(object.last_height) : Long.ZERO,
        blocksToStall: isSet(object.blocks_to_stall) ? Long.fromValue(object.blocks_to_stall) : Long.ZERO,
      };
    },

    toJSON(message: EventPriceStaleWarning): unknown {
      const obj: any = {};
      if (message.source !== "") {
        obj.source = message.source;
      }
      if (message.id !== undefined) {
        obj.id = DataID.toJSON(message.id);
      }
      if (!message.lastHeight.equals(Long.ZERO)) {
        obj.last_height = (message.lastHeight || Long.ZERO).toString();
      }
      if (!message.blocksToStall.equals(Long.ZERO)) {
        obj.blocks_to_stall = (message.blocksToStall || Long.ZERO).toString();
      }
      return obj;
    },

    create(base?: DeepPartial<EventPriceStaleWarning>): EventPriceStaleWarning {
      return EventPriceStaleWarning.fromPartial(base ?? {});
    },
    fromPartial(object: DeepPartial<EventPriceStaleWarning>): EventPriceStaleWarning {
      const message = createBaseEventPriceStaleWarning();
      message.source = object.source ?? "";
      message.id = (object.id !== undefined && object.id !== null) ? DataID.fromPartial(object.id) : undefined;
      message.lastHeight = (object.lastHeight !== undefined && object.lastHeight !== null)
        ? Long.fromValue(object.lastHeight)
        : Long.ZERO;
      message.blocksToStall = (object.blocksToStall !== undefined && object.blocksToStall !== null)
        ? Long.fromValue(object.blocksToStall)
        : Long.ZERO;
      return message;
    },
  };

function createBaseEventPriceStaled(): EventPriceStaled {
  return { source: "", id: undefined, lastHeight: Long.ZERO };
}

export const EventPriceStaled: MessageFns<EventPriceStaled, "virtengine.oracle.v1.EventPriceStaled"> = {
  $type: "virtengine.oracle.v1.EventPriceStaled" as const,

  encode(message: EventPriceStaled, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.id !== undefined) {
      DataID.encode(message.id, writer.uint32(18).fork()).join();
    }
    if (!message.lastHeight.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.lastHeight.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPriceStaled {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPriceStaled();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = DataID.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.lastHeight = Long.fromString(reader.int64().toString());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPriceStaled {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      id: isSet(object.id) ? DataID.fromJSON(object.id) : undefined,
      lastHeight: isSet(object.last_height) ? Long.fromValue(object.last_height) : Long.ZERO,
    };
  },

  toJSON(message: EventPriceStaled): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.id !== undefined) {
      obj.id = DataID.toJSON(message.id);
    }
    if (!message.lastHeight.equals(Long.ZERO)) {
      obj.last_height = (message.lastHeight || Long.ZERO).toString();
    }
    return obj;
  },

  create(base?: DeepPartial<EventPriceStaled>): EventPriceStaled {
    return EventPriceStaled.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventPriceStaled>): EventPriceStaled {
    const message = createBaseEventPriceStaled();
    message.source = object.source ?? "";
    message.id = (object.id !== undefined && object.id !== null) ? DataID.fromPartial(object.id) : undefined;
    message.lastHeight = (object.lastHeight !== undefined && object.lastHeight !== null)
      ? Long.fromValue(object.lastHeight)
      : Long.ZERO;
    return message;
  },
};

function createBaseEventPriceRecovered(): EventPriceRecovered {
  return { source: "", id: undefined, height: Long.ZERO };
}

export const EventPriceRecovered: MessageFns<EventPriceRecovered, "virtengine.oracle.v1.EventPriceRecovered"> = {
  $type: "virtengine.oracle.v1.EventPriceRecovered" as const,

  encode(message: EventPriceRecovered, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.id !== undefined) {
      DataID.encode(message.id, writer.uint32(18).fork()).join();
    }
    if (!message.height.equals(Long.ZERO)) {
      writer.uint32(24).int64(message.height.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPriceRecovered {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPriceRecovered();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.id = DataID.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.height = Long.fromString(reader.int64().toString());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPriceRecovered {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      id: isSet(object.id) ? DataID.fromJSON(object.id) : undefined,
      height: isSet(object.height) ? Long.fromValue(object.height) : Long.ZERO,
    };
  },

  toJSON(message: EventPriceRecovered): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.id !== undefined) {
      obj.id = DataID.toJSON(message.id);
    }
    if (!message.height.equals(Long.ZERO)) {
      obj.height = (message.height || Long.ZERO).toString();
    }
    return obj;
  },

  create(base?: DeepPartial<EventPriceRecovered>): EventPriceRecovered {
    return EventPriceRecovered.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventPriceRecovered>): EventPriceRecovered {
    const message = createBaseEventPriceRecovered();
    message.source = object.source ?? "";
    message.id = (object.id !== undefined && object.id !== null) ? DataID.fromPartial(object.id) : undefined;
    message.height = (object.height !== undefined && object.height !== null)
      ? Long.fromValue(object.height)
      : Long.ZERO;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
