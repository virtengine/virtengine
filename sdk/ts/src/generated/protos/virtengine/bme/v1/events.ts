import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/bme/v1/events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import { LedgerRecordID, MintStatus, mintStatusFromJSON, mintStatusToJSON } from "./types.ts";

/** EventCircuitBreakerStatusChange is emitted when circuit breaker status changes */
export interface EventMintStatusChange {
  /** previous_status is the previous status */
  previousStatus: MintStatus;
  /** new_status is the new status */
  newStatus: MintStatus;
  /** collateral_ratio is the CR that triggered the change */
  collateralRatio: string;
}

/** EventVaultSeeded is emitted when the vault is seeded with AKT */
export interface EventVaultSeeded {
  /** amount is the AKT amount added to vault */
  amount:
    | Coin
    | undefined;
  /** source is where the funds came from */
  source: string;
  /** new_vault_balance is the new vault balance */
  newVaultBalance: Coin | undefined;
}

/** EventLedgerRecordExecuted emitted information of burn/mint event of token A burn to mint token B */
export interface EventLedgerRecordExecuted {
  /** burned_from source address of the tokens burned */
  id: LedgerRecordID | undefined;
}

function createBaseEventMintStatusChange(): EventMintStatusChange {
  return { previousStatus: 0, newStatus: 0, collateralRatio: "" };
}

export const EventMintStatusChange: MessageFns<EventMintStatusChange, "virtengine.bme.v1.EventMintStatusChange"> = {
  $type: "virtengine.bme.v1.EventMintStatusChange" as const,

  encode(message: EventMintStatusChange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.previousStatus !== 0) {
      writer.uint32(8).int32(message.previousStatus);
    }
    if (message.newStatus !== 0) {
      writer.uint32(16).int32(message.newStatus);
    }
    if (message.collateralRatio !== "") {
      writer.uint32(26).string(message.collateralRatio);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventMintStatusChange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventMintStatusChange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.previousStatus = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.newStatus = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.collateralRatio = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventMintStatusChange {
    return {
      previousStatus: isSet(object.previous_status) ? mintStatusFromJSON(object.previous_status) : 0,
      newStatus: isSet(object.new_status) ? mintStatusFromJSON(object.new_status) : 0,
      collateralRatio: isSet(object.collateral_ratio) ? globalThis.String(object.collateral_ratio) : "",
    };
  },

  toJSON(message: EventMintStatusChange): unknown {
    const obj: any = {};
    if (message.previousStatus !== 0) {
      obj.previous_status = mintStatusToJSON(message.previousStatus);
    }
    if (message.newStatus !== 0) {
      obj.new_status = mintStatusToJSON(message.newStatus);
    }
    if (message.collateralRatio !== "") {
      obj.collateral_ratio = message.collateralRatio;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventMintStatusChange>): EventMintStatusChange {
    const message = createBaseEventMintStatusChange();
    message.previousStatus = object.previousStatus ?? 0;
    message.newStatus = object.newStatus ?? 0;
    message.collateralRatio = object.collateralRatio ?? "";
    return message;
  },
};

function createBaseEventVaultSeeded(): EventVaultSeeded {
  return { amount: undefined, source: "", newVaultBalance: undefined };
}

export const EventVaultSeeded: MessageFns<EventVaultSeeded, "virtengine.bme.v1.EventVaultSeeded"> = {
  $type: "virtengine.bme.v1.EventVaultSeeded" as const,

  encode(message: EventVaultSeeded, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.amount !== undefined) {
      Coin.encode(message.amount, writer.uint32(10).fork()).join();
    }
    if (message.source !== "") {
      writer.uint32(18).string(message.source);
    }
    if (message.newVaultBalance !== undefined) {
      Coin.encode(message.newVaultBalance, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventVaultSeeded {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventVaultSeeded();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.amount = Coin.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newVaultBalance = Coin.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventVaultSeeded {
    return {
      amount: isSet(object.amount) ? Coin.fromJSON(object.amount) : undefined,
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      newVaultBalance: isSet(object.new_vault_balance) ? Coin.fromJSON(object.new_vault_balance) : undefined,
    };
  },

  toJSON(message: EventVaultSeeded): unknown {
    const obj: any = {};
    if (message.amount !== undefined) {
      obj.amount = Coin.toJSON(message.amount);
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.newVaultBalance !== undefined) {
      obj.new_vault_balance = Coin.toJSON(message.newVaultBalance);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventVaultSeeded>): EventVaultSeeded {
    const message = createBaseEventVaultSeeded();
    message.amount = (object.amount !== undefined && object.amount !== null)
      ? Coin.fromPartial(object.amount)
      : undefined;
    message.source = object.source ?? "";
    message.newVaultBalance = (object.newVaultBalance !== undefined && object.newVaultBalance !== null)
      ? Coin.fromPartial(object.newVaultBalance)
      : undefined;
    return message;
  },
};

function createBaseEventLedgerRecordExecuted(): EventLedgerRecordExecuted {
  return { id: undefined };
}

export const EventLedgerRecordExecuted: MessageFns<
  EventLedgerRecordExecuted,
  "virtengine.bme.v1.EventLedgerRecordExecuted"
> = {
  $type: "virtengine.bme.v1.EventLedgerRecordExecuted" as const,

  encode(message: EventLedgerRecordExecuted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      LedgerRecordID.encode(message.id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventLedgerRecordExecuted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventLedgerRecordExecuted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = LedgerRecordID.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventLedgerRecordExecuted {
    return { id: isSet(object.id) ? LedgerRecordID.fromJSON(object.id) : undefined };
  },

  toJSON(message: EventLedgerRecordExecuted): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = LedgerRecordID.toJSON(message.id);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventLedgerRecordExecuted>): EventLedgerRecordExecuted {
    const message = createBaseEventLedgerRecordExecuted();
    message.id = (object.id !== undefined && object.id !== null) ? LedgerRecordID.fromPartial(object.id) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
