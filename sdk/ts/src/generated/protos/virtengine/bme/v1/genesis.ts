// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/bme/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Coin } from "../../../cosmos/base/v1beta1/coin.ts";
import { Params } from "./params.ts";
import { LedgerPendingRecord, LedgerRecord, LedgerRecordID } from "./types.ts";

/** GenesisLedgerRecord */
export interface GenesisLedgerRecord {
  id: LedgerRecordID | undefined;
  record: LedgerRecord | undefined;
}

/** GenesisLedgerPendingRecord */
export interface GenesisLedgerPendingRecord {
  id: LedgerRecordID | undefined;
  record: LedgerPendingRecord | undefined;
}

/** GenesisLedgerState */
export interface GenesisLedgerState {
  records: GenesisLedgerRecord[];
  pendingRecords: GenesisLedgerPendingRecord[];
}

/** GenesisVaultState */
export interface GenesisVaultState {
  /** burned is the cumulative burn for tracked tokens */
  totalBurned: Coin[];
  /** minted is the cumulative mint back for tracked tokens */
  totalMinted: Coin[];
  /**
   * remint_credits tracks available credits for reminting tokens
   * (e.g., from previous burns that can be reminted without additional collateral)
   */
  remintCredits: Coin[];
}

/** GenesisState defines the BME module's genesis state */
export interface GenesisState {
  /** params defines the module parameters */
  params:
    | Params
    | undefined;
  /** state is the initial vault state */
  state: GenesisVaultState | undefined;
  ledger: GenesisLedgerState | undefined;
}

function createBaseGenesisLedgerRecord(): GenesisLedgerRecord {
  return { id: undefined, record: undefined };
}

export const GenesisLedgerRecord: MessageFns<GenesisLedgerRecord, "virtengine.bme.v1.GenesisLedgerRecord"> = {
  $type: "virtengine.bme.v1.GenesisLedgerRecord" as const,

  encode(message: GenesisLedgerRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      LedgerRecordID.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.record !== undefined) {
      LedgerRecord.encode(message.record, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisLedgerRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisLedgerRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = LedgerRecordID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.record = LedgerRecord.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisLedgerRecord {
    return {
      id: isSet(object.id) ? LedgerRecordID.fromJSON(object.id) : undefined,
      record: isSet(object.record) ? LedgerRecord.fromJSON(object.record) : undefined,
    };
  },

  toJSON(message: GenesisLedgerRecord): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = LedgerRecordID.toJSON(message.id);
    }
    if (message.record !== undefined) {
      obj.record = LedgerRecord.toJSON(message.record);
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisLedgerRecord>): GenesisLedgerRecord {
    return GenesisLedgerRecord.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisLedgerRecord>): GenesisLedgerRecord {
    const message = createBaseGenesisLedgerRecord();
    message.id = (object.id !== undefined && object.id !== null) ? LedgerRecordID.fromPartial(object.id) : undefined;
    message.record = (object.record !== undefined && object.record !== null)
      ? LedgerRecord.fromPartial(object.record)
      : undefined;
    return message;
  },
};

function createBaseGenesisLedgerPendingRecord(): GenesisLedgerPendingRecord {
  return { id: undefined, record: undefined };
}

export const GenesisLedgerPendingRecord: MessageFns<
  GenesisLedgerPendingRecord,
  "virtengine.bme.v1.GenesisLedgerPendingRecord"
> = {
  $type: "virtengine.bme.v1.GenesisLedgerPendingRecord" as const,

  encode(message: GenesisLedgerPendingRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      LedgerRecordID.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.record !== undefined) {
      LedgerPendingRecord.encode(message.record, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisLedgerPendingRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisLedgerPendingRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = LedgerRecordID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.record = LedgerPendingRecord.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisLedgerPendingRecord {
    return {
      id: isSet(object.id) ? LedgerRecordID.fromJSON(object.id) : undefined,
      record: isSet(object.record) ? LedgerPendingRecord.fromJSON(object.record) : undefined,
    };
  },

  toJSON(message: GenesisLedgerPendingRecord): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = LedgerRecordID.toJSON(message.id);
    }
    if (message.record !== undefined) {
      obj.record = LedgerPendingRecord.toJSON(message.record);
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisLedgerPendingRecord>): GenesisLedgerPendingRecord {
    return GenesisLedgerPendingRecord.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisLedgerPendingRecord>): GenesisLedgerPendingRecord {
    const message = createBaseGenesisLedgerPendingRecord();
    message.id = (object.id !== undefined && object.id !== null) ? LedgerRecordID.fromPartial(object.id) : undefined;
    message.record = (object.record !== undefined && object.record !== null)
      ? LedgerPendingRecord.fromPartial(object.record)
      : undefined;
    return message;
  },
};

function createBaseGenesisLedgerState(): GenesisLedgerState {
  return { records: [], pendingRecords: [] };
}

export const GenesisLedgerState: MessageFns<GenesisLedgerState, "virtengine.bme.v1.GenesisLedgerState"> = {
  $type: "virtengine.bme.v1.GenesisLedgerState" as const,

  encode(message: GenesisLedgerState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.records) {
      GenesisLedgerRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.pendingRecords) {
      GenesisLedgerPendingRecord.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisLedgerState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisLedgerState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.records.push(GenesisLedgerRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pendingRecords.push(GenesisLedgerPendingRecord.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisLedgerState {
    return {
      records: globalThis.Array.isArray(object?.records)
        ? object.records.map((e: any) => GenesisLedgerRecord.fromJSON(e))
        : [],
      pendingRecords: globalThis.Array.isArray(object?.pending_records)
        ? object.pending_records.map((e: any) => GenesisLedgerPendingRecord.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisLedgerState): unknown {
    const obj: any = {};
    if (message.records?.length) {
      obj.records = message.records.map((e) => GenesisLedgerRecord.toJSON(e));
    }
    if (message.pendingRecords?.length) {
      obj.pending_records = message.pendingRecords.map((e) => GenesisLedgerPendingRecord.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisLedgerState>): GenesisLedgerState {
    return GenesisLedgerState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisLedgerState>): GenesisLedgerState {
    const message = createBaseGenesisLedgerState();
    message.records = object.records?.map((e) => GenesisLedgerRecord.fromPartial(e)) || [];
    message.pendingRecords = object.pendingRecords?.map((e) => GenesisLedgerPendingRecord.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGenesisVaultState(): GenesisVaultState {
  return { totalBurned: [], totalMinted: [], remintCredits: [] };
}

export const GenesisVaultState: MessageFns<GenesisVaultState, "virtengine.bme.v1.GenesisVaultState"> = {
  $type: "virtengine.bme.v1.GenesisVaultState" as const,

  encode(message: GenesisVaultState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.totalBurned) {
      Coin.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.totalMinted) {
      Coin.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.remintCredits) {
      Coin.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisVaultState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisVaultState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.totalBurned.push(Coin.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.totalMinted.push(Coin.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.remintCredits.push(Coin.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisVaultState {
    return {
      totalBurned: globalThis.Array.isArray(object?.total_burned)
        ? object.total_burned.map((e: any) => Coin.fromJSON(e))
        : [],
      totalMinted: globalThis.Array.isArray(object?.total_minted)
        ? object.total_minted.map((e: any) => Coin.fromJSON(e))
        : [],
      remintCredits: globalThis.Array.isArray(object?.remint_credits)
        ? object.remint_credits.map((e: any) => Coin.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GenesisVaultState): unknown {
    const obj: any = {};
    if (message.totalBurned?.length) {
      obj.total_burned = message.totalBurned.map((e) => Coin.toJSON(e));
    }
    if (message.totalMinted?.length) {
      obj.total_minted = message.totalMinted.map((e) => Coin.toJSON(e));
    }
    if (message.remintCredits?.length) {
      obj.remint_credits = message.remintCredits.map((e) => Coin.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisVaultState>): GenesisVaultState {
    return GenesisVaultState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisVaultState>): GenesisVaultState {
    const message = createBaseGenesisVaultState();
    message.totalBurned = object.totalBurned?.map((e) => Coin.fromPartial(e)) || [];
    message.totalMinted = object.totalMinted?.map((e) => Coin.fromPartial(e)) || [];
    message.remintCredits = object.remintCredits?.map((e) => Coin.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGenesisState(): GenesisState {
  return { params: undefined, state: undefined, ledger: undefined };
}

export const GenesisState: MessageFns<GenesisState, "virtengine.bme.v1.GenesisState"> = {
  $type: "virtengine.bme.v1.GenesisState" as const,

  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    if (message.state !== undefined) {
      GenesisVaultState.encode(message.state, writer.uint32(18).fork()).join();
    }
    if (message.ledger !== undefined) {
      GenesisLedgerState.encode(message.ledger, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.state = GenesisVaultState.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ledger = GenesisLedgerState.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      state: isSet(object.state) ? GenesisVaultState.fromJSON(object.state) : undefined,
      ledger: isSet(object.ledger) ? GenesisLedgerState.fromJSON(object.ledger) : undefined,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.state !== undefined) {
      obj.state = GenesisVaultState.toJSON(message.state);
    }
    if (message.ledger !== undefined) {
      obj.ledger = GenesisLedgerState.toJSON(message.ledger);
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.state = (object.state !== undefined && object.state !== null)
      ? GenesisVaultState.fromPartial(object.state)
      : undefined;
    message.ledger = (object.ledger !== undefined && object.ledger !== null)
      ? GenesisLedgerState.fromPartial(object.ledger)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
