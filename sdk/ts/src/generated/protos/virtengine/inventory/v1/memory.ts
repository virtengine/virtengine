import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/inventory/v1/memory.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { ResourcePair } from "./resourcepair.ts";

/** MemoryInfo reports Memory details */
export interface MemoryInfo {
  vendor: string;
  type: string;
  totalSize: string;
  speed: string;
}

/** Memory reports Memory inventory details */
export interface Memory {
  quantity: ResourcePair | undefined;
  info: MemoryInfo[];
}

function createBaseMemoryInfo(): MemoryInfo {
  return { vendor: "", type: "", totalSize: "", speed: "" };
}

export const MemoryInfo: MessageFns<MemoryInfo, "virtengine.inventory.v1.MemoryInfo"> = {
  $type: "virtengine.inventory.v1.MemoryInfo" as const,

  encode(message: MemoryInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vendor !== "") {
      writer.uint32(10).string(message.vendor);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.totalSize !== "") {
      writer.uint32(26).string(message.totalSize);
    }
    if (message.speed !== "") {
      writer.uint32(34).string(message.speed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MemoryInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMemoryInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vendor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.totalSize = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.speed = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MemoryInfo {
    return {
      vendor: isSet(object.vendor) ? globalThis.String(object.vendor) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      totalSize: isSet(object.total_size) ? globalThis.String(object.total_size) : "",
      speed: isSet(object.speed) ? globalThis.String(object.speed) : "",
    };
  },

  toJSON(message: MemoryInfo): unknown {
    const obj: any = {};
    if (message.vendor !== "") {
      obj.vendor = message.vendor;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.totalSize !== "") {
      obj.total_size = message.totalSize;
    }
    if (message.speed !== "") {
      obj.speed = message.speed;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<MemoryInfo>): MemoryInfo {
    const message = createBaseMemoryInfo();
    message.vendor = object.vendor ?? "";
    message.type = object.type ?? "";
    message.totalSize = object.totalSize ?? "";
    message.speed = object.speed ?? "";
    return message;
  },
};

function createBaseMemory(): Memory {
  return { quantity: undefined, info: [] };
}

export const Memory: MessageFns<Memory, "virtengine.inventory.v1.Memory"> = {
  $type: "virtengine.inventory.v1.Memory" as const,

  encode(message: Memory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.quantity !== undefined) {
      ResourcePair.encode(message.quantity, writer.uint32(10).fork()).join();
    }
    for (const v of message.info) {
      MemoryInfo.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Memory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMemory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.quantity = ResourcePair.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.info.push(MemoryInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Memory {
    return {
      quantity: isSet(object.quantity) ? ResourcePair.fromJSON(object.quantity) : undefined,
      info: globalThis.Array.isArray(object?.info) ? object.info.map((e: any) => MemoryInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: Memory): unknown {
    const obj: any = {};
    if (message.quantity !== undefined) {
      obj.quantity = ResourcePair.toJSON(message.quantity);
    }
    if (message.info?.length) {
      obj.info = message.info.map((e) => MemoryInfo.toJSON(e));
    }
    return obj;
  },
  fromPartial(object: DeepPartial<Memory>): Memory {
    const message = createBaseMemory();
    message.quantity = (object.quantity !== undefined && object.quantity !== null)
      ? ResourcePair.fromPartial(object.quantity)
      : undefined;
    message.info = object.info?.map((e) => MemoryInfo.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
