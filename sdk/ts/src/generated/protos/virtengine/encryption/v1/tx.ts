import { bytesFromBase64, base64FromBytes, isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/encryption/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

/** MsgRegisterRecipientKey is the message for registering a recipient public key */
export interface MsgRegisterRecipientKey {
  /** Sender is the account registering the key (must match the key owner) */
  sender: string;
  /** PublicKey is the X25519 public key bytes (32 bytes) */
  publicKey: Uint8Array;
  /** AlgorithmID specifies which algorithm this key is for */
  algorithmId: string;
  /** Label is an optional human-readable label for the key */
  label: string;
}

/** MsgRegisterRecipientKeyResponse is the response for MsgRegisterRecipientKey */
export interface MsgRegisterRecipientKeyResponse {
  /** KeyFingerprint is the fingerprint of the registered key */
  keyFingerprint: string;
}

/** MsgRevokeRecipientKey is the message for revoking a recipient public key */
export interface MsgRevokeRecipientKey {
  /** Sender is the account revoking the key (must own the key) */
  sender: string;
  /** KeyFingerprint is the fingerprint of the key to revoke */
  keyFingerprint: string;
}

/** MsgRevokeRecipientKeyResponse is the response for MsgRevokeRecipientKey */
export interface MsgRevokeRecipientKeyResponse {
}

/** MsgUpdateKeyLabel is the message for updating a key's label */
export interface MsgUpdateKeyLabel {
  /** Sender is the account updating the key (must own the key) */
  sender: string;
  /** KeyFingerprint is the fingerprint of the key to update */
  keyFingerprint: string;
  /** Label is the new label for the key */
  label: string;
}

/** MsgUpdateKeyLabelResponse is the response for MsgUpdateKeyLabel */
export interface MsgUpdateKeyLabelResponse {
}

function createBaseMsgRegisterRecipientKey(): MsgRegisterRecipientKey {
  return { sender: "", publicKey: new Uint8Array(0), algorithmId: "", label: "" };
}

export const MsgRegisterRecipientKey: MessageFns<
  MsgRegisterRecipientKey,
  "virtengine.encryption.v1.MsgRegisterRecipientKey"
> = {
  $type: "virtengine.encryption.v1.MsgRegisterRecipientKey" as const,

  encode(message: MsgRegisterRecipientKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(18).bytes(message.publicKey);
    }
    if (message.algorithmId !== "") {
      writer.uint32(26).string(message.algorithmId);
    }
    if (message.label !== "") {
      writer.uint32(34).string(message.label);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterRecipientKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterRecipientKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.algorithmId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.label = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterRecipientKey {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      publicKey: isSet(object.public_key) ? bytesFromBase64(object.public_key) : new Uint8Array(0),
      algorithmId: isSet(object.algorithm_id) ? globalThis.String(object.algorithm_id) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
    };
  },

  toJSON(message: MsgRegisterRecipientKey): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.publicKey.length !== 0) {
      obj.public_key = base64FromBytes(message.publicKey);
    }
    if (message.algorithmId !== "") {
      obj.algorithm_id = message.algorithmId;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<MsgRegisterRecipientKey>): MsgRegisterRecipientKey {
    const message = createBaseMsgRegisterRecipientKey();
    message.sender = object.sender ?? "";
    message.publicKey = object.publicKey ?? new Uint8Array(0);
    message.algorithmId = object.algorithmId ?? "";
    message.label = object.label ?? "";
    return message;
  },
};

function createBaseMsgRegisterRecipientKeyResponse(): MsgRegisterRecipientKeyResponse {
  return { keyFingerprint: "" };
}

export const MsgRegisterRecipientKeyResponse: MessageFns<
  MsgRegisterRecipientKeyResponse,
  "virtengine.encryption.v1.MsgRegisterRecipientKeyResponse"
> = {
  $type: "virtengine.encryption.v1.MsgRegisterRecipientKeyResponse" as const,

  encode(message: MsgRegisterRecipientKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.keyFingerprint !== "") {
      writer.uint32(10).string(message.keyFingerprint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterRecipientKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterRecipientKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keyFingerprint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterRecipientKeyResponse {
    return { keyFingerprint: isSet(object.key_fingerprint) ? globalThis.String(object.key_fingerprint) : "" };
  },

  toJSON(message: MsgRegisterRecipientKeyResponse): unknown {
    const obj: any = {};
    if (message.keyFingerprint !== "") {
      obj.key_fingerprint = message.keyFingerprint;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<MsgRegisterRecipientKeyResponse>): MsgRegisterRecipientKeyResponse {
    const message = createBaseMsgRegisterRecipientKeyResponse();
    message.keyFingerprint = object.keyFingerprint ?? "";
    return message;
  },
};

function createBaseMsgRevokeRecipientKey(): MsgRevokeRecipientKey {
  return { sender: "", keyFingerprint: "" };
}

export const MsgRevokeRecipientKey: MessageFns<
  MsgRevokeRecipientKey,
  "virtengine.encryption.v1.MsgRevokeRecipientKey"
> = {
  $type: "virtengine.encryption.v1.MsgRevokeRecipientKey" as const,

  encode(message: MsgRevokeRecipientKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.keyFingerprint !== "") {
      writer.uint32(18).string(message.keyFingerprint);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRevokeRecipientKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeRecipientKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.keyFingerprint = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRevokeRecipientKey {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      keyFingerprint: isSet(object.key_fingerprint) ? globalThis.String(object.key_fingerprint) : "",
    };
  },

  toJSON(message: MsgRevokeRecipientKey): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.keyFingerprint !== "") {
      obj.key_fingerprint = message.keyFingerprint;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<MsgRevokeRecipientKey>): MsgRevokeRecipientKey {
    const message = createBaseMsgRevokeRecipientKey();
    message.sender = object.sender ?? "";
    message.keyFingerprint = object.keyFingerprint ?? "";
    return message;
  },
};

function createBaseMsgRevokeRecipientKeyResponse(): MsgRevokeRecipientKeyResponse {
  return {};
}

export const MsgRevokeRecipientKeyResponse: MessageFns<
  MsgRevokeRecipientKeyResponse,
  "virtengine.encryption.v1.MsgRevokeRecipientKeyResponse"
> = {
  $type: "virtengine.encryption.v1.MsgRevokeRecipientKeyResponse" as const,

  encode(_: MsgRevokeRecipientKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRevokeRecipientKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeRecipientKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRevokeRecipientKeyResponse {
    return {};
  },

  toJSON(_: MsgRevokeRecipientKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },
  fromPartial(_: DeepPartial<MsgRevokeRecipientKeyResponse>): MsgRevokeRecipientKeyResponse {
    const message = createBaseMsgRevokeRecipientKeyResponse();
    return message;
  },
};

function createBaseMsgUpdateKeyLabel(): MsgUpdateKeyLabel {
  return { sender: "", keyFingerprint: "", label: "" };
}

export const MsgUpdateKeyLabel: MessageFns<MsgUpdateKeyLabel, "virtengine.encryption.v1.MsgUpdateKeyLabel"> = {
  $type: "virtengine.encryption.v1.MsgUpdateKeyLabel" as const,

  encode(message: MsgUpdateKeyLabel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sender !== "") {
      writer.uint32(10).string(message.sender);
    }
    if (message.keyFingerprint !== "") {
      writer.uint32(18).string(message.keyFingerprint);
    }
    if (message.label !== "") {
      writer.uint32(26).string(message.label);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateKeyLabel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateKeyLabel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.keyFingerprint = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.label = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateKeyLabel {
    return {
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      keyFingerprint: isSet(object.key_fingerprint) ? globalThis.String(object.key_fingerprint) : "",
      label: isSet(object.label) ? globalThis.String(object.label) : "",
    };
  },

  toJSON(message: MsgUpdateKeyLabel): unknown {
    const obj: any = {};
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.keyFingerprint !== "") {
      obj.key_fingerprint = message.keyFingerprint;
    }
    if (message.label !== "") {
      obj.label = message.label;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<MsgUpdateKeyLabel>): MsgUpdateKeyLabel {
    const message = createBaseMsgUpdateKeyLabel();
    message.sender = object.sender ?? "";
    message.keyFingerprint = object.keyFingerprint ?? "";
    message.label = object.label ?? "";
    return message;
  },
};

function createBaseMsgUpdateKeyLabelResponse(): MsgUpdateKeyLabelResponse {
  return {};
}

export const MsgUpdateKeyLabelResponse: MessageFns<
  MsgUpdateKeyLabelResponse,
  "virtengine.encryption.v1.MsgUpdateKeyLabelResponse"
> = {
  $type: "virtengine.encryption.v1.MsgUpdateKeyLabelResponse" as const,

  encode(_: MsgUpdateKeyLabelResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateKeyLabelResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateKeyLabelResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateKeyLabelResponse {
    return {};
  },

  toJSON(_: MsgUpdateKeyLabelResponse): unknown {
    const obj: any = {};
    return obj;
  },
  fromPartial(_: DeepPartial<MsgUpdateKeyLabelResponse>): MsgUpdateKeyLabelResponse {
    const message = createBaseMsgUpdateKeyLabelResponse();
    return message;
  },
};

function _unused_bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function _unused_base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
