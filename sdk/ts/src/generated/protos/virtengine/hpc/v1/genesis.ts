import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/hpc/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import {
  HPCCluster,
  HPCDispute,
  HPCJob,
  HPCOffering,
  HPCRewardRecord,
  JobAccounting,
  NodeMetadata,
  Params,
  SchedulingDecision,
} from "./types.ts";

/** GenesisState defines the hpc module's genesis state */
export interface GenesisState {
  /** Params are the module parameters */
  params:
    | Params
    | undefined;
  /** Clusters are the registered HPC clusters */
  clusters: HPCCluster[];
  /** Offerings are the HPC offerings */
  offerings: HPCOffering[];
  /** Jobs are the HPC jobs */
  jobs: HPCJob[];
  /** JobAccountings are the job accounting records */
  jobAccountings: JobAccounting[];
  /** NodeMetadatas are the node metadata records */
  nodeMetadatas: NodeMetadata[];
  /** SchedulingDecisions are the scheduling decision records */
  schedulingDecisions: SchedulingDecision[];
  /** HPCRewards are the HPC reward records */
  hpcRewards: HPCRewardRecord[];
  /** Disputes are the dispute records */
  disputes: HPCDispute[];
  /** ClusterSequence is the next cluster sequence */
  clusterSequence: Long;
  /** OfferingSequence is the next offering sequence */
  offeringSequence: Long;
  /** JobSequence is the next job sequence */
  jobSequence: Long;
  /** DecisionSequence is the next decision sequence */
  decisionSequence: Long;
  /** DisputeSequence is the next dispute sequence */
  disputeSequence: Long;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    clusters: [],
    offerings: [],
    jobs: [],
    jobAccountings: [],
    nodeMetadatas: [],
    schedulingDecisions: [],
    hpcRewards: [],
    disputes: [],
    clusterSequence: Long.UZERO,
    offeringSequence: Long.UZERO,
    jobSequence: Long.UZERO,
    decisionSequence: Long.UZERO,
    disputeSequence: Long.UZERO,
  };
}

export const GenesisState: MessageFns<GenesisState, "virtengine.hpc.v1.GenesisState"> = {
  $type: "virtengine.hpc.v1.GenesisState" as const,

  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.clusters) {
      HPCCluster.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.offerings) {
      HPCOffering.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.jobs) {
      HPCJob.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.jobAccountings) {
      JobAccounting.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.nodeMetadatas) {
      NodeMetadata.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.schedulingDecisions) {
      SchedulingDecision.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.hpcRewards) {
      HPCRewardRecord.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.disputes) {
      HPCDispute.encode(v!, writer.uint32(74).fork()).join();
    }
    if (!message.clusterSequence.equals(Long.UZERO)) {
      writer.uint32(80).uint64(message.clusterSequence.toString());
    }
    if (!message.offeringSequence.equals(Long.UZERO)) {
      writer.uint32(88).uint64(message.offeringSequence.toString());
    }
    if (!message.jobSequence.equals(Long.UZERO)) {
      writer.uint32(96).uint64(message.jobSequence.toString());
    }
    if (!message.decisionSequence.equals(Long.UZERO)) {
      writer.uint32(104).uint64(message.decisionSequence.toString());
    }
    if (!message.disputeSequence.equals(Long.UZERO)) {
      writer.uint32(112).uint64(message.disputeSequence.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clusters.push(HPCCluster.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.offerings.push(HPCOffering.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.jobs.push(HPCJob.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.jobAccountings.push(JobAccounting.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.nodeMetadatas.push(NodeMetadata.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.schedulingDecisions.push(SchedulingDecision.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.hpcRewards.push(HPCRewardRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.disputes.push(HPCDispute.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.clusterSequence = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.offeringSequence = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.jobSequence = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.decisionSequence = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.disputeSequence = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      clusters: globalThis.Array.isArray(object?.clusters)
        ? object.clusters.map((e: any) => HPCCluster.fromJSON(e))
        : [],
      offerings: globalThis.Array.isArray(object?.offerings)
        ? object.offerings.map((e: any) => HPCOffering.fromJSON(e))
        : [],
      jobs: globalThis.Array.isArray(object?.jobs) ? object.jobs.map((e: any) => HPCJob.fromJSON(e)) : [],
      jobAccountings: globalThis.Array.isArray(object?.job_accountings)
        ? object.job_accountings.map((e: any) => JobAccounting.fromJSON(e))
        : [],
      nodeMetadatas: globalThis.Array.isArray(object?.node_metadatas)
        ? object.node_metadatas.map((e: any) => NodeMetadata.fromJSON(e))
        : [],
      schedulingDecisions: globalThis.Array.isArray(object?.scheduling_decisions)
        ? object.scheduling_decisions.map((e: any) => SchedulingDecision.fromJSON(e))
        : [],
      hpcRewards: globalThis.Array.isArray(object?.hpc_rewards)
        ? object.hpc_rewards.map((e: any) => HPCRewardRecord.fromJSON(e))
        : [],
      disputes: globalThis.Array.isArray(object?.disputes)
        ? object.disputes.map((e: any) => HPCDispute.fromJSON(e))
        : [],
      clusterSequence: isSet(object.cluster_sequence) ? Long.fromValue(object.cluster_sequence) : Long.UZERO,
      offeringSequence: isSet(object.offering_sequence) ? Long.fromValue(object.offering_sequence) : Long.UZERO,
      jobSequence: isSet(object.job_sequence) ? Long.fromValue(object.job_sequence) : Long.UZERO,
      decisionSequence: isSet(object.decision_sequence) ? Long.fromValue(object.decision_sequence) : Long.UZERO,
      disputeSequence: isSet(object.dispute_sequence) ? Long.fromValue(object.dispute_sequence) : Long.UZERO,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.clusters?.length) {
      obj.clusters = message.clusters.map((e) => HPCCluster.toJSON(e));
    }
    if (message.offerings?.length) {
      obj.offerings = message.offerings.map((e) => HPCOffering.toJSON(e));
    }
    if (message.jobs?.length) {
      obj.jobs = message.jobs.map((e) => HPCJob.toJSON(e));
    }
    if (message.jobAccountings?.length) {
      obj.job_accountings = message.jobAccountings.map((e) => JobAccounting.toJSON(e));
    }
    if (message.nodeMetadatas?.length) {
      obj.node_metadatas = message.nodeMetadatas.map((e) => NodeMetadata.toJSON(e));
    }
    if (message.schedulingDecisions?.length) {
      obj.scheduling_decisions = message.schedulingDecisions.map((e) => SchedulingDecision.toJSON(e));
    }
    if (message.hpcRewards?.length) {
      obj.hpc_rewards = message.hpcRewards.map((e) => HPCRewardRecord.toJSON(e));
    }
    if (message.disputes?.length) {
      obj.disputes = message.disputes.map((e) => HPCDispute.toJSON(e));
    }
    if (!message.clusterSequence.equals(Long.UZERO)) {
      obj.cluster_sequence = (message.clusterSequence || Long.UZERO).toString();
    }
    if (!message.offeringSequence.equals(Long.UZERO)) {
      obj.offering_sequence = (message.offeringSequence || Long.UZERO).toString();
    }
    if (!message.jobSequence.equals(Long.UZERO)) {
      obj.job_sequence = (message.jobSequence || Long.UZERO).toString();
    }
    if (!message.decisionSequence.equals(Long.UZERO)) {
      obj.decision_sequence = (message.decisionSequence || Long.UZERO).toString();
    }
    if (!message.disputeSequence.equals(Long.UZERO)) {
      obj.dispute_sequence = (message.disputeSequence || Long.UZERO).toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.clusters = object.clusters?.map((e) => HPCCluster.fromPartial(e)) || [];
    message.offerings = object.offerings?.map((e) => HPCOffering.fromPartial(e)) || [];
    message.jobs = object.jobs?.map((e) => HPCJob.fromPartial(e)) || [];
    message.jobAccountings = object.jobAccountings?.map((e) => JobAccounting.fromPartial(e)) || [];
    message.nodeMetadatas = object.nodeMetadatas?.map((e) => NodeMetadata.fromPartial(e)) || [];
    message.schedulingDecisions = object.schedulingDecisions?.map((e) => SchedulingDecision.fromPartial(e)) || [];
    message.hpcRewards = object.hpcRewards?.map((e) => HPCRewardRecord.fromPartial(e)) || [];
    message.disputes = object.disputes?.map((e) => HPCDispute.fromPartial(e)) || [];
    message.clusterSequence = (object.clusterSequence !== undefined && object.clusterSequence !== null)
      ? Long.fromValue(object.clusterSequence)
      : Long.UZERO;
    message.offeringSequence = (object.offeringSequence !== undefined && object.offeringSequence !== null)
      ? Long.fromValue(object.offeringSequence)
      : Long.UZERO;
    message.jobSequence = (object.jobSequence !== undefined && object.jobSequence !== null)
      ? Long.fromValue(object.jobSequence)
      : Long.UZERO;
    message.decisionSequence = (object.decisionSequence !== undefined && object.decisionSequence !== null)
      ? Long.fromValue(object.decisionSequence)
      : Long.UZERO;
    message.disputeSequence = (object.disputeSequence !== undefined && object.disputeSequence !== null)
      ? Long.fromValue(object.disputeSequence)
      : Long.UZERO;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
