// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/downtimedetector/v1beta1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Timestamp } from "../../../google/protobuf/timestamp.ts";
import { Downtime, downtimeFromJSON, downtimeToJSON } from "./downtime_duration.ts";

/** GenesisDowntimeEntry tracks the last occurrence of a specific downtime duration */
export interface GenesisDowntimeEntry {
  /** duration is the downtime period being tracked */
  duration: Downtime;
  /** last_downtime is the timestamp when this downtime duration was last observed */
  lastDowntime: Date | undefined;
}

/** GenesisState defines the downtime detector module's genesis state */
export interface GenesisState {
  /** downtimes is the list of tracked downtime entries */
  downtimes: GenesisDowntimeEntry[];
  /** last_block_time is the timestamp of the last processed block */
  lastBlockTime: Date | undefined;
}

function createBaseGenesisDowntimeEntry(): GenesisDowntimeEntry {
  return { duration: 0, lastDowntime: undefined };
}

export const GenesisDowntimeEntry: MessageFns<
  GenesisDowntimeEntry,
  "virtengine.downtimedetector.v1beta1.GenesisDowntimeEntry"
> = {
  $type: "virtengine.downtimedetector.v1beta1.GenesisDowntimeEntry" as const,

  encode(message: GenesisDowntimeEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.duration !== 0) {
      writer.uint32(8).int32(message.duration);
    }
    if (message.lastDowntime !== undefined) {
      Timestamp.encode(toTimestamp(message.lastDowntime), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisDowntimeEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisDowntimeEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.duration = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lastDowntime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisDowntimeEntry {
    return {
      duration: isSet(object.duration) ? downtimeFromJSON(object.duration) : 0,
      lastDowntime: isSet(object.last_downtime) ? fromJsonTimestamp(object.last_downtime) : undefined,
    };
  },

  toJSON(message: GenesisDowntimeEntry): unknown {
    const obj: any = {};
    if (message.duration !== 0) {
      obj.duration = downtimeToJSON(message.duration);
    }
    if (message.lastDowntime !== undefined) {
      obj.last_downtime = message.lastDowntime.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisDowntimeEntry>): GenesisDowntimeEntry {
    return GenesisDowntimeEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisDowntimeEntry>): GenesisDowntimeEntry {
    const message = createBaseGenesisDowntimeEntry();
    message.duration = object.duration ?? 0;
    message.lastDowntime = object.lastDowntime ?? undefined;
    return message;
  },
};

function createBaseGenesisState(): GenesisState {
  return { downtimes: [], lastBlockTime: undefined };
}

export const GenesisState: MessageFns<GenesisState, "virtengine.downtimedetector.v1beta1.GenesisState"> = {
  $type: "virtengine.downtimedetector.v1beta1.GenesisState" as const,

  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.downtimes) {
      GenesisDowntimeEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.lastBlockTime !== undefined) {
      Timestamp.encode(toTimestamp(message.lastBlockTime), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.downtimes.push(GenesisDowntimeEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lastBlockTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      downtimes: globalThis.Array.isArray(object?.downtimes)
        ? object.downtimes.map((e: any) => GenesisDowntimeEntry.fromJSON(e))
        : [],
      lastBlockTime: isSet(object.last_block_time) ? fromJsonTimestamp(object.last_block_time) : undefined,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.downtimes?.length) {
      obj.downtimes = message.downtimes.map((e) => GenesisDowntimeEntry.toJSON(e));
    }
    if (message.lastBlockTime !== undefined) {
      obj.last_block_time = message.lastBlockTime.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<GenesisState>): GenesisState {
    return GenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.downtimes = object.downtimes?.map((e) => GenesisDowntimeEntry.fromPartial(e)) || [];
    message.lastBlockTime = object.lastBlockTime ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
