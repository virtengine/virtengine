import { bytesFromBase64, base64FromBytes, isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/deployment/v1/event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { DeploymentID } from "./deployment.ts";
import { GroupID } from "./group.ts";

/**
 * EventDeploymentCreated event is triggered when deployment is created on chain.
 * It contains all the information required to identify a deployment.
 */
export interface EventDeploymentCreated {
  /** ID is the unique identifier of the deployment. */
  id:
    | DeploymentID
    | undefined;
  /** Hash is an hashed representation of the deployment. */
  hash: Uint8Array;
}

/**
 * EventDeploymentUpdated is triggered when deployment is updated on chain.
 * It contains all the information required to identify a deployment.
 */
export interface EventDeploymentUpdated {
  /** ID is the unique identifier of the deployment. */
  id:
    | DeploymentID
    | undefined;
  /** Hash is an hashed representation of the deployment. */
  hash: Uint8Array;
}

/**
 * EventDeploymentClosed is triggered when deployment is closed on chain.
 * It contains all the information required to identify a deployment.
 */
export interface EventDeploymentClosed {
  /** ID is the unique identifier of the deployment. */
  id: DeploymentID | undefined;
}

/**
 * EventGroupStarted is triggered when deployment group is started.
 * It contains all the information required to identify a group.
 */
export interface EventGroupStarted {
  /** ID is the unique identifier of the group. */
  id: GroupID | undefined;
}

/**
 * EventGroupPaused is triggered when deployment group is paused.
 * It contains all the information required to identify a group.
 */
export interface EventGroupPaused {
  /** ID is the unique identifier of the group. */
  id: GroupID | undefined;
}

/**
 * EventGroupClosed is triggered when deployment group is closed.
 * It contains all the information required to identify a group.
 */
export interface EventGroupClosed {
  /** ID is the unique identifier of the group. */
  id: GroupID | undefined;
}

function createBaseEventDeploymentCreated(): EventDeploymentCreated {
  return { id: undefined, hash: new Uint8Array(0) };
}

export const EventDeploymentCreated: MessageFns<
  EventDeploymentCreated,
  "virtengine.deployment.v1.EventDeploymentCreated"
> = {
  $type: "virtengine.deployment.v1.EventDeploymentCreated" as const,

  encode(message: EventDeploymentCreated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      DeploymentID.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.hash.length !== 0) {
      writer.uint32(18).bytes(message.hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventDeploymentCreated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDeploymentCreated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = DeploymentID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDeploymentCreated {
    return {
      id: isSet(object.id) ? DeploymentID.fromJSON(object.id) : undefined,
      hash: isSet(object.hash) ? bytesFromBase64(object.hash) : new Uint8Array(0),
    };
  },

  toJSON(message: EventDeploymentCreated): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = DeploymentID.toJSON(message.id);
    }
    if (message.hash.length !== 0) {
      obj.hash = base64FromBytes(message.hash);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventDeploymentCreated>): EventDeploymentCreated {
    const message = createBaseEventDeploymentCreated();
    message.id = (object.id !== undefined && object.id !== null) ? DeploymentID.fromPartial(object.id) : undefined;
    message.hash = object.hash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEventDeploymentUpdated(): EventDeploymentUpdated {
  return { id: undefined, hash: new Uint8Array(0) };
}

export const EventDeploymentUpdated: MessageFns<
  EventDeploymentUpdated,
  "virtengine.deployment.v1.EventDeploymentUpdated"
> = {
  $type: "virtengine.deployment.v1.EventDeploymentUpdated" as const,

  encode(message: EventDeploymentUpdated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      DeploymentID.encode(message.id, writer.uint32(10).fork()).join();
    }
    if (message.hash.length !== 0) {
      writer.uint32(18).bytes(message.hash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventDeploymentUpdated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDeploymentUpdated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = DeploymentID.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDeploymentUpdated {
    return {
      id: isSet(object.id) ? DeploymentID.fromJSON(object.id) : undefined,
      hash: isSet(object.hash) ? bytesFromBase64(object.hash) : new Uint8Array(0),
    };
  },

  toJSON(message: EventDeploymentUpdated): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = DeploymentID.toJSON(message.id);
    }
    if (message.hash.length !== 0) {
      obj.hash = base64FromBytes(message.hash);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventDeploymentUpdated>): EventDeploymentUpdated {
    const message = createBaseEventDeploymentUpdated();
    message.id = (object.id !== undefined && object.id !== null) ? DeploymentID.fromPartial(object.id) : undefined;
    message.hash = object.hash ?? new Uint8Array(0);
    return message;
  },
};

function createBaseEventDeploymentClosed(): EventDeploymentClosed {
  return { id: undefined };
}

export const EventDeploymentClosed: MessageFns<
  EventDeploymentClosed,
  "virtengine.deployment.v1.EventDeploymentClosed"
> = {
  $type: "virtengine.deployment.v1.EventDeploymentClosed" as const,

  encode(message: EventDeploymentClosed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      DeploymentID.encode(message.id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventDeploymentClosed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventDeploymentClosed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = DeploymentID.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventDeploymentClosed {
    return { id: isSet(object.id) ? DeploymentID.fromJSON(object.id) : undefined };
  },

  toJSON(message: EventDeploymentClosed): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = DeploymentID.toJSON(message.id);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventDeploymentClosed>): EventDeploymentClosed {
    const message = createBaseEventDeploymentClosed();
    message.id = (object.id !== undefined && object.id !== null) ? DeploymentID.fromPartial(object.id) : undefined;
    return message;
  },
};

function createBaseEventGroupStarted(): EventGroupStarted {
  return { id: undefined };
}

export const EventGroupStarted: MessageFns<EventGroupStarted, "virtengine.deployment.v1.EventGroupStarted"> = {
  $type: "virtengine.deployment.v1.EventGroupStarted" as const,

  encode(message: EventGroupStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      GroupID.encode(message.id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventGroupStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventGroupStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = GroupID.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventGroupStarted {
    return { id: isSet(object.id) ? GroupID.fromJSON(object.id) : undefined };
  },

  toJSON(message: EventGroupStarted): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = GroupID.toJSON(message.id);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventGroupStarted>): EventGroupStarted {
    const message = createBaseEventGroupStarted();
    message.id = (object.id !== undefined && object.id !== null) ? GroupID.fromPartial(object.id) : undefined;
    return message;
  },
};

function createBaseEventGroupPaused(): EventGroupPaused {
  return { id: undefined };
}

export const EventGroupPaused: MessageFns<EventGroupPaused, "virtengine.deployment.v1.EventGroupPaused"> = {
  $type: "virtengine.deployment.v1.EventGroupPaused" as const,

  encode(message: EventGroupPaused, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      GroupID.encode(message.id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventGroupPaused {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventGroupPaused();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = GroupID.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventGroupPaused {
    return { id: isSet(object.id) ? GroupID.fromJSON(object.id) : undefined };
  },

  toJSON(message: EventGroupPaused): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = GroupID.toJSON(message.id);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventGroupPaused>): EventGroupPaused {
    const message = createBaseEventGroupPaused();
    message.id = (object.id !== undefined && object.id !== null) ? GroupID.fromPartial(object.id) : undefined;
    return message;
  },
};

function createBaseEventGroupClosed(): EventGroupClosed {
  return { id: undefined };
}

export const EventGroupClosed: MessageFns<EventGroupClosed, "virtengine.deployment.v1.EventGroupClosed"> = {
  $type: "virtengine.deployment.v1.EventGroupClosed" as const,

  encode(message: EventGroupClosed, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== undefined) {
      GroupID.encode(message.id, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventGroupClosed {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventGroupClosed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = GroupID.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventGroupClosed {
    return { id: isSet(object.id) ? GroupID.fromJSON(object.id) : undefined };
  },

  toJSON(message: EventGroupClosed): unknown {
    const obj: any = {};
    if (message.id !== undefined) {
      obj.id = GroupID.toJSON(message.id);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<EventGroupClosed>): EventGroupClosed {
    const message = createBaseEventGroupClosed();
    message.id = (object.id !== undefined && object.id !== null) ? GroupID.fromPartial(object.id) : undefined;
    return message;
  },
};

function _unused_bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function _unused_base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
