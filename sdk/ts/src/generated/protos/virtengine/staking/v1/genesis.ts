import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/staking/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Params } from "./params.ts";
import { RewardEpoch, SlashRecord, ValidatorPerformance, ValidatorReward, ValidatorSigningInfo } from "./types.ts";

/** GenesisState is the genesis state for the staking module */
export interface GenesisState {
  /** Params are the module parameters */
  params:
    | Params
    | undefined;
  /** ValidatorPerformances are the initial validator performances */
  validatorPerformances: ValidatorPerformance[];
  /** SlashRecords are the initial slashing records */
  slashRecords: SlashRecord[];
  /** RewardEpochs are the initial reward epochs */
  rewardEpochs: RewardEpoch[];
  /** ValidatorRewards are the initial validator rewards */
  validatorRewards: ValidatorReward[];
  /** ValidatorSigningInfos are the initial signing infos */
  validatorSigningInfos: ValidatorSigningInfo[];
  /** CurrentEpoch is the current epoch number */
  currentEpoch: Long;
  /** SlashSequence is the next slashing sequence number */
  slashSequence: Long;
}

function createBaseGenesisState(): GenesisState {
  return {
    params: undefined,
    validatorPerformances: [],
    slashRecords: [],
    rewardEpochs: [],
    validatorRewards: [],
    validatorSigningInfos: [],
    currentEpoch: Long.UZERO,
    slashSequence: Long.UZERO,
  };
}

export const GenesisState: MessageFns<GenesisState, "virtengine.staking.v1.GenesisState"> = {
  $type: "virtengine.staking.v1.GenesisState" as const,

  encode(message: GenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.validatorPerformances) {
      ValidatorPerformance.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.slashRecords) {
      SlashRecord.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.rewardEpochs) {
      RewardEpoch.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.validatorRewards) {
      ValidatorReward.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.validatorSigningInfos) {
      ValidatorSigningInfo.encode(v!, writer.uint32(50).fork()).join();
    }
    if (!message.currentEpoch.equals(Long.UZERO)) {
      writer.uint32(56).uint64(message.currentEpoch.toString());
    }
    if (!message.slashSequence.equals(Long.UZERO)) {
      writer.uint32(64).uint64(message.slashSequence.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.validatorPerformances.push(ValidatorPerformance.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.slashRecords.push(SlashRecord.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rewardEpochs.push(RewardEpoch.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.validatorRewards.push(ValidatorReward.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.validatorSigningInfos.push(ValidatorSigningInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.currentEpoch = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.slashSequence = Long.fromString(reader.uint64().toString(), true);
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenesisState {
    return {
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
      validatorPerformances: globalThis.Array.isArray(object?.validator_performances)
        ? object.validator_performances.map((e: any) => ValidatorPerformance.fromJSON(e))
        : [],
      slashRecords: globalThis.Array.isArray(object?.slash_records)
        ? object.slash_records.map((e: any) => SlashRecord.fromJSON(e))
        : [],
      rewardEpochs: globalThis.Array.isArray(object?.reward_epochs)
        ? object.reward_epochs.map((e: any) => RewardEpoch.fromJSON(e))
        : [],
      validatorRewards: globalThis.Array.isArray(object?.validator_rewards)
        ? object.validator_rewards.map((e: any) => ValidatorReward.fromJSON(e))
        : [],
      validatorSigningInfos: globalThis.Array.isArray(object?.validator_signing_infos)
        ? object.validator_signing_infos.map((e: any) => ValidatorSigningInfo.fromJSON(e))
        : [],
      currentEpoch: isSet(object.current_epoch) ? Long.fromValue(object.current_epoch) : Long.UZERO,
      slashSequence: isSet(object.slash_sequence) ? Long.fromValue(object.slash_sequence) : Long.UZERO,
    };
  },

  toJSON(message: GenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    if (message.validatorPerformances?.length) {
      obj.validator_performances = message.validatorPerformances.map((e) => ValidatorPerformance.toJSON(e));
    }
    if (message.slashRecords?.length) {
      obj.slash_records = message.slashRecords.map((e) => SlashRecord.toJSON(e));
    }
    if (message.rewardEpochs?.length) {
      obj.reward_epochs = message.rewardEpochs.map((e) => RewardEpoch.toJSON(e));
    }
    if (message.validatorRewards?.length) {
      obj.validator_rewards = message.validatorRewards.map((e) => ValidatorReward.toJSON(e));
    }
    if (message.validatorSigningInfos?.length) {
      obj.validator_signing_infos = message.validatorSigningInfos.map((e) => ValidatorSigningInfo.toJSON(e));
    }
    if (!message.currentEpoch.equals(Long.UZERO)) {
      obj.current_epoch = (message.currentEpoch || Long.UZERO).toString();
    }
    if (!message.slashSequence.equals(Long.UZERO)) {
      obj.slash_sequence = (message.slashSequence || Long.UZERO).toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<GenesisState>): GenesisState {
    const message = createBaseGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    message.validatorPerformances = object.validatorPerformances?.map((e) => ValidatorPerformance.fromPartial(e)) || [];
    message.slashRecords = object.slashRecords?.map((e) => SlashRecord.fromPartial(e)) || [];
    message.rewardEpochs = object.rewardEpochs?.map((e) => RewardEpoch.fromPartial(e)) || [];
    message.validatorRewards = object.validatorRewards?.map((e) => ValidatorReward.fromPartial(e)) || [];
    message.validatorSigningInfos = object.validatorSigningInfos?.map((e) => ValidatorSigningInfo.fromPartial(e)) || [];
    message.currentEpoch = (object.currentEpoch !== undefined && object.currentEpoch !== null)
      ? Long.fromValue(object.currentEpoch)
      : Long.UZERO;
    message.slashSequence = (object.slashSequence !== undefined && object.slashSequence !== null)
      ? Long.fromValue(object.slashSequence)
      : Long.UZERO;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
