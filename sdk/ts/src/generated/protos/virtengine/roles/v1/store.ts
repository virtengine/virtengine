import { isSet } from "../../../../../encoding/typeEncodingHelpers.ts"
import type { DeepPartial, MessageFns } from "../../../../../encoding/typeEncodingHelpers.ts"
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               unknown
// source: virtengine/roles/v1/store.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

/** ParamsStore is the internal storage format for module parameters */
export interface ParamsStore {
  /** MaxRolesPerAccount is the maximum number of roles an account can have */
  maxRolesPerAccount: number;
  /** AllowSelfRevoke determines if accounts can revoke their own roles */
  allowSelfRevoke: boolean;
}

/** RoleAssignmentStore is the internal storage format for role assignments */
export interface RoleAssignmentStore {
  /** AssignedBy is the address that assigned this role */
  assignedBy: string;
  /** AssignedAt is the Unix timestamp when the role was assigned */
  assignedAt: Long;
}

/** AccountStateStore is the internal storage format for account states */
export interface AccountStateStore {
  /** State is the current account state (as uint32 for storage) */
  state: number;
  /** Reason is the reason for the current state */
  reason: string;
  /** ModifiedBy is the address that last modified the state */
  modifiedBy: string;
  /** ModifiedAt is the Unix timestamp when the state was last modified */
  modifiedAt: Long;
  /** PreviousState is the previous account state (as uint32 for storage) */
  previousState: number;
}

function createBaseParamsStore(): ParamsStore {
  return { maxRolesPerAccount: 0, allowSelfRevoke: false };
}

export const ParamsStore: MessageFns<ParamsStore, "virtengine.roles.v1.ParamsStore"> = {
  $type: "virtengine.roles.v1.ParamsStore" as const,

  encode(message: ParamsStore, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxRolesPerAccount !== 0) {
      writer.uint32(8).uint32(message.maxRolesPerAccount);
    }
    if (message.allowSelfRevoke !== false) {
      writer.uint32(16).bool(message.allowSelfRevoke);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParamsStore {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParamsStore();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxRolesPerAccount = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.allowSelfRevoke = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParamsStore {
    return {
      maxRolesPerAccount: isSet(object.max_roles_per_account) ? globalThis.Number(object.max_roles_per_account) : 0,
      allowSelfRevoke: isSet(object.allow_self_revoke) ? globalThis.Boolean(object.allow_self_revoke) : false,
    };
  },

  toJSON(message: ParamsStore): unknown {
    const obj: any = {};
    if (message.maxRolesPerAccount !== 0) {
      obj.max_roles_per_account = Math.round(message.maxRolesPerAccount);
    }
    if (message.allowSelfRevoke !== false) {
      obj.allow_self_revoke = message.allowSelfRevoke;
    }
    return obj;
  },
  fromPartial(object: DeepPartial<ParamsStore>): ParamsStore {
    const message = createBaseParamsStore();
    message.maxRolesPerAccount = object.maxRolesPerAccount ?? 0;
    message.allowSelfRevoke = object.allowSelfRevoke ?? false;
    return message;
  },
};

function createBaseRoleAssignmentStore(): RoleAssignmentStore {
  return { assignedBy: "", assignedAt: Long.ZERO };
}

export const RoleAssignmentStore: MessageFns<RoleAssignmentStore, "virtengine.roles.v1.RoleAssignmentStore"> = {
  $type: "virtengine.roles.v1.RoleAssignmentStore" as const,

  encode(message: RoleAssignmentStore, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assignedBy !== "") {
      writer.uint32(10).string(message.assignedBy);
    }
    if (!message.assignedAt.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.assignedAt.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoleAssignmentStore {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoleAssignmentStore();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.assignedBy = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.assignedAt = Long.fromString(reader.int64().toString());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoleAssignmentStore {
    return {
      assignedBy: isSet(object.assigned_by) ? globalThis.String(object.assigned_by) : "",
      assignedAt: isSet(object.assigned_at) ? Long.fromValue(object.assigned_at) : Long.ZERO,
    };
  },

  toJSON(message: RoleAssignmentStore): unknown {
    const obj: any = {};
    if (message.assignedBy !== "") {
      obj.assigned_by = message.assignedBy;
    }
    if (!message.assignedAt.equals(Long.ZERO)) {
      obj.assigned_at = (message.assignedAt || Long.ZERO).toString();
    }
    return obj;
  },
  fromPartial(object: DeepPartial<RoleAssignmentStore>): RoleAssignmentStore {
    const message = createBaseRoleAssignmentStore();
    message.assignedBy = object.assignedBy ?? "";
    message.assignedAt = (object.assignedAt !== undefined && object.assignedAt !== null)
      ? Long.fromValue(object.assignedAt)
      : Long.ZERO;
    return message;
  },
};

function createBaseAccountStateStore(): AccountStateStore {
  return { state: 0, reason: "", modifiedBy: "", modifiedAt: Long.ZERO, previousState: 0 };
}

export const AccountStateStore: MessageFns<AccountStateStore, "virtengine.roles.v1.AccountStateStore"> = {
  $type: "virtengine.roles.v1.AccountStateStore" as const,

  encode(message: AccountStateStore, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).uint32(message.state);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    if (message.modifiedBy !== "") {
      writer.uint32(26).string(message.modifiedBy);
    }
    if (!message.modifiedAt.equals(Long.ZERO)) {
      writer.uint32(32).int64(message.modifiedAt.toString());
    }
    if (message.previousState !== 0) {
      writer.uint32(40).uint32(message.previousState);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AccountStateStore {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccountStateStore();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.modifiedBy = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.modifiedAt = Long.fromString(reader.int64().toString());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.previousState = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AccountStateStore {
    return {
      state: isSet(object.state) ? globalThis.Number(object.state) : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      modifiedBy: isSet(object.modified_by) ? globalThis.String(object.modified_by) : "",
      modifiedAt: isSet(object.modified_at) ? Long.fromValue(object.modified_at) : Long.ZERO,
      previousState: isSet(object.previous_state) ? globalThis.Number(object.previous_state) : 0,
    };
  },

  toJSON(message: AccountStateStore): unknown {
    const obj: any = {};
    if (message.state !== 0) {
      obj.state = Math.round(message.state);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.modifiedBy !== "") {
      obj.modified_by = message.modifiedBy;
    }
    if (!message.modifiedAt.equals(Long.ZERO)) {
      obj.modified_at = (message.modifiedAt || Long.ZERO).toString();
    }
    if (message.previousState !== 0) {
      obj.previous_state = Math.round(message.previousState);
    }
    return obj;
  },
  fromPartial(object: DeepPartial<AccountStateStore>): AccountStateStore {
    const message = createBaseAccountStateStore();
    message.state = object.state ?? 0;
    message.reason = object.reason ?? "";
    message.modifiedBy = object.modifiedBy ?? "";
    message.modifiedAt = (object.modifiedAt !== undefined && object.modifiedAt !== null)
      ? Long.fromValue(object.modifiedAt)
      : Long.ZERO;
    message.previousState = object.previousState ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type _unused_DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function _unused_isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface _unused_MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
