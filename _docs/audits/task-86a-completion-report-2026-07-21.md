# Task 86A Reproducible Contract Completion Report

**Date:** 2026-07-21
**Branch:** `stable-virtengine-beta`
**Status:** locally implemented; Docker-image build and public live-node all-route certification remain environment-blocked

## Scope and inventory

The machine-readable source of truth is `sdk/artifacts/proto/inventory.json` with a SHA-256 companion. It records:

- 6 `go.work` modules plus 1 isolated generator tool module;
- 23 replace directives;
- 245 protobuf files;
- 65 services (32 query, 29 message, 4 other provider services);
- 404 RPC methods and 213 HTTP bindings;
- 228 generated Go protobuf files;
- 28 concrete Go gateway files and 0 production `gateway_stub.go` files;
- 637 generated TypeScript files (including one retained dotted-path k8s compatibility artifact);
- 213 protobuf-derived OpenAPI paths/operations with 213 unique operation IDs and source-fragment trace metadata.

The inventory also records generated output paths, query/message method input/output types, gRPC paths, HTTP methods/routes, Go package options, workspace/checksum/vendor policy, and unsupported-language fail-closed policy.

## Dependency and generation foundation

- Existing application and SDK checksums already contain the prior missing `glog`, `spdystream`, and `go-flowrate` entries; no manual `go.sum` edit or unrelated upgrade was made.
- `sdk/generation/go.mod` and `go.sum` freeze Buf, the VirtEngine gocosmos replacement, grpc-gateway/swagger, and modvendor.
- `sdk/generation/Dockerfile` pins Go 1.25.5 Bookworm by OCI digest and verifies protoc/Node downloads by SHA-256 for Linux amd64/arm64.
- `sdk/generation/toolchain.json` is the machine-readable tool/output manifest.
- `sdk/buf.gen.go.yaml`, `sdk/buf.gen.openapi.yaml`, and `sdk/buf.gen.ts.yaml` declare the supported source graph. Plugin paths are absolute inside the supported container. The WSL equivalent builds tools into an isolated absolute cache and verifies its Node archive digest.
- Python and Rust proto outputs are not declared release contracts and fail closed. Existing experimental templates are not treated as fresh or publishable evidence.
- The separate manual provider API generator now pins Redocly 2.39.0, openapi-typescript 7.13.0, and oapi-codegen 2.3.0; mutable `latest` execution was removed.

## Generated outputs and compatibility

- All 14 no-op gateway stubs were deleted. Twenty-eight descriptor-generated gateway implementations now exist, including HPC and marketplace.
- The generated descriptor set is `sdk/artifacts/proto/virtengine.binpb` with SHA-256 `e35cc0a7f6a4fc8a78a25ee962b0bcede9c48dae40683d414427616766465c43`.
- `api/openapi/virtengine-proto.swagger.json` is composed deterministically from generated fragments. `portal_api.yaml` remains manual because it documents non-proto provider services.
- Task 84A's modified VEID proto and `MsgSubmitConsensusVerification`/vote-extension schema were preserved and regenerated byte-identically from source.
- `tests/compatibility/task86a_contract_compatibility_test.go` freezes legacy binary bytes, proves `Any` resolution/packing for registered interface implementations, and verifies module default-genesis determinism.
- `tests/gateway/task86a_routes_test.go` crosses a real generated HTTP gateway into gRPC query clients for previously stubbed HPC and marketplace routes. It does not call keepers as its acceptance boundary.

## TDD evidence

**RED:** `node --test scripts/protoinventory/inventory.test.mjs` failed with `ERR_MODULE_NOT_FOUND` because the inventory parser did not exist.
**GREEN:** after implementing deterministic service/method/HTTP parsing and duplicate-route validation, both tests passed.
**REFACTOR:** recursive traversal was sorted, ignored build/vendor trees were excluded, workspace and tool modules were classified separately, and the generated inventory remained deterministic.

Compatibility tests initially exposed an invalid hand-copied fixture and empty genesis values that are valid for some modules. The fixture was regenerated from the legacy type, empty genesis was compared byte-for-byte, and all Task 86A compatibility tests passed.

## Local validation record

Passed locally:

- pinned WSL Go gateway generation, twice: second-run changed files `0`;
- protobuf OpenAPI generation, twice: SHA-256 `16dd49e02c120ad796f87a39d511f84e92dfe59446ac6f2f79518e753264692d` both runs;
- frozen TypeScript generation/build, twice: second-run changed files `0`;
- `go test -p 1 ./sdk/go/node/... -run '^$'`;
- `go test -p 1 ./tests/compatibility -run '^TestTask86A' -count=1`;
- `go test -p 1 -tags=e2e.integration ./tests/gateway -run '^TestTask86A' -count=1`;
- TypeScript `npm run build` under Node 24.12.0 after `npm ci` from the lockfile.

TypeScript `npm test -- --runInBand` is not green: 33/36 suites and 1089/1101 tests pass. The three Buf-backed generator harness suites fail because their inline configs include local Cosmos/gogoproto module paths while the parent `sdk/buf.yaml` also resolves the same BSR modules (`contained in multiple modules`). This harness conflict is reported as a remaining validation gap; it is not represented as a generated-client source failure.

`buf build` succeeds and produced the descriptor. `buf lint` still reports legacy comment/enum-name findings across existing schemas. Renaming deployed enum values to satisfy style lint would violate the wire/API preservation rule; this is reported rather than misrepresented as green.

## Remaining blockers and non-claims

- Docker Desktop daemon is unavailable locally, so the digest-pinned image itself was not built/run. The same scripts/tools were exercised through WSL Linux/amd64.
- A public live node was not available. Local gateway tests cross generated HTTP-to-gRPC boundaries in-process, but this report does not claim every one of the 213 routes was exercised against a separately running node.
- Buf breaking comparison needs an approved prior release descriptor baseline. The current descriptor is now checked in for future comparisons; no unsupported historical-baseline claim is made.
- Full repository lint/build/preflight may report active Task 84A and unrelated inference/security work. Those files were preserved and are not attributed to Task 86A.
