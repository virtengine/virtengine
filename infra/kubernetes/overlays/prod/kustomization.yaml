# Compatibility shim only.
# Canonical production rendering lives under deploy/kubernetes/overlays/prod.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../deploy/kubernetes/overlays/prod
