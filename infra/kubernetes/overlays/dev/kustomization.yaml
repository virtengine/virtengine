# Compatibility shim only.
# Canonical development rendering lives under deploy/kubernetes/overlays/dev.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../deploy/kubernetes/overlays/dev
