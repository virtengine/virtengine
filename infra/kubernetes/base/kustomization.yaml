# Compatibility shim only.
# Canonical application resources live under deploy/kubernetes/base.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../deploy/kubernetes/base
