Status 2026-09-19 run 130: ROOT-CAUSED + FIXED (not flake; deterministic env divergence).
Cause: get_region() interpolated raw AWS-metadata-endpoint bytes into JSON metadata.
On GH runners (Azure VMs) that endpoint answers with an error body, so region =
JSON-breaking garbage -> metadata invalid -> jq fails (chain metadata region =
file line 7, 2x non-fatal; provider metadata region = file line 11, 1x fatal:
create_backup aborts under set -e, no backup name, smoke-test grep+pipefail -> exit 1).
Local WSL green: no IMDS -> hostname fallback -> valid JSON.
Repro: stub curl emitting an error body -> exact CI signature (2x line-7, chain
success line, 1x line-11, exit 1). Post-fix rerun of same repro: GREEN.
Fix: validated get_region (charset + hostname/unknown fallbacks, --max-time 5) in
backup-chain-state.sh, backup-provider-state.sh, dr-test.sh; metadata + webhook
JSON via jq -n/--arg; smoke test fails with captured backup log on empty name.
Verified: poisoned-curl GREEN, hostile-garbage GREEN, clean GREEN, metadata sane
(height numeric 1000), bash -n OK, shellcheck only pre-existing warnings.
Remaining: push branch + PR + confirm green on main (local-only contract).
