"""Vendored third-party Python dependencies.

Each subpackage here is a pinned copy of an upstream release that cannot be
installed from PyPI against this project's pinned toolchain. See the ``README.md``
next to each vendored package for its provenance and the exact patches applied.
"""
